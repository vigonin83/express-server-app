<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta http-equiv="X-UA-Compatible" content="IE=edge">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
   <title>Server toturial</title>

</head>
<body>
   <% if (created) { %>
      <div class="alert alert-success">
         Note has been created!
      </div>
   <% } %>

   <div class="mx-auto" style="width: 600px">
      <h1><%= title %></h1>
      <form action="/" method="post">
         <div class="form-group mb-2">
            <input type="text" class="form-control" name="title" required>
         </div>
         <button class="btn btn-primary mb-2" type="submit">Create</button>
      </form>
      <% if(notes.length) { %>
      <div class="card">
         <ul class="list-group">
            <% for(let i = 0; i < notes.length; i++) { %>
            <li class="list-group-item d-flex align-items-center justify-content-between">
              <%= notes[i].title %>
              <div>
                <button class="btn btn-primary me-2" data-type="update" data-id="<%= notes[i].id %>">Редактировать</button>
                <button class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id %>">&times;</button>
              </div>
            </li>
            <% } %>
         </ul>
      </div>
      <% } else { %>
         <p>No notes</p>
      <% } %>
   </div>
   <script src="/app.js"></script>
</body>
</html>